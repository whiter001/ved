# Ved 项目目录结构说明文档

`Ved` 是一个使用 V 语言编写的轻量级、高性能图形化文本编辑器。该项目采用模块化设计，核心渲染基于 V 语言的 `gg` 图形库。

## 1. 核心源代码文件 (根目录)

| 文件名 | 含义与功能 |
| :--- | :--- |
| `ved.v` | **主入口程序**。定义了核心 `Ved` 结构体，管理全局状态（如工作区、分屏、模式切换）、初始化窗口及主循环逻辑。 |
| `view.v` | **视图管理**。负责单个文件缓冲区的状态，包括文件路径、内容行、光标坐标、滚动位置及断点管理。 |
| `draw.v` | **图形渲染逻辑**。处理所有 UI 组件的绘制，包括文本渲染、光标效果、状态栏、标题栏及各种浮窗界面。 |
| `events.v` | **事件分发**。处理鼠标点击、滚轮滚动以及复杂的键盘输入逻辑（普通、插入、视觉模式的键位映射）。 |
| `config.v` | **配置与皮肤**。定义了编辑器配置项及配色方案（Base16 风格），负责解析 `conf.toml` 设置。 |
| `syntax.v` | **语法高亮定义**。负责加载和解析 `.syntax` 文件（JSON 格式），管理关键字和扩展名匹配规则。 |
| `hl.v` | **语法高亮执行**。核心高亮分析逻辑，将文本分解为不同类型的 Chunk（字符串、注释、关键字）以供绘制。 |
| `query.v` | **交互式搜索**。实现了 Ctrl+P (Fuzzy Finder)、全局 Grep、行内搜索等搜索窗口的交互逻辑。 |
| `cmd.v` | **外部命令集成**。支持在编辑器内直接运行构建脚本、Shell 命令，并将输出结果捕获到编辑器视图中。 |
| `autocomplete.v` | **代码补全**。简单的代码建议窗口逻辑，支持根据当前上下文提供补全选项。 |
| `file_tree.v` | **文件系统树**。实现侧边栏文件浏览功能，管理目录的展开、折叠及层级结构。 |
| `goto.v` | **代码导航**。实现“跳转到定义”（Go to definition）和在函数起始点之间快速跳转的功能。 |
| `gq.v` | **文本重排**。移植了 Vim 的 `gq` 功能，用于将视觉选中的段落按指定宽度进行格式化。 |
| `timer.v` | **番茄钟工具**。内置的时间管理系统，支持任务记录和 Pomodoro 计时功能。 |
| `debugger.v` | **调试接口**。提供初步的调试器集成支持，允许在编辑器内控制 V 程序的调试流程。 |
| `util.v` | **通用工具**。包含缩进处理、字符串分析等跨模块共享的辅助函数。 |
| `ruler.v` | **排版辅助**。用于测试字体渲染尺寸和视图对齐的标尺工具。 |

## 2. 关键子目录

| 目录名 | 含义与功能 |
| :--- | :--- |
| `syntax/` | 预设语法高亮定义文件夹。包含 `v.syntax` 等语言定义文件。 |
| `debugger/` | 包含调试功能的配套脚本和示例代码。 |
| `uiold/` | 平台特定集成模块。例如在 macOS 下注册全局“唤醒”热键的底层实现。 |
| `vid.app/` | macOS 应用程序打包资源，定义了 App 的图标、标识等元数据。 |

## 3. 其他重要文件

| 文件名 | 含义与功能 |
| :--- | :--- |
| `v.mod` | V 项目模块描述文件（版本、依赖、元数据）。 |
| `README.md` | 项目使用说明、功能列表及编译指南。 |
| `LICENSE` | GPL-3.0 授权协议。 |
| `TODO.txt` | 开发进度跟踪与待办功能列表。 |
| `RobotoMono-Regular.ttf` | 编辑器默认内置的等宽字体文件。 |
| `welcome.txt` | 编辑器启动时的欢迎界面内容。 |

## 4. 核心概念

### 工作区 (Workspaces)
Ved 提倡以“工作区”为单位进行代码管理（即项目根目录）。
*   支持同时打开多个工作区：`ved path/to/project1 path/to/project2`。
*   使用 `C [` 和 `C ]` 在不同项目间秒级切换。

### 项目级配置 (`.ved`)
在工作区根目录下添加 `.ved` (JSON 格式) 文件可以自定义特定项目的行为：
```json
{
    "grep_file_extensions": ["go", "v"] // 限制全局搜索 (Grep) 仅扫描指定后缀的文件
}
```

## 5. 常用快捷键说明

注：`C` 在 macOS 上指代 `⌘ (Command)`，在其他系统上指代 `Ctrl`。

### 核心功能
| 快捷键 | 动作 |
| :--- | :--- |
| **`C o`** | 打开文件 (Open file) |
| **`C s`** | 保存当前文件 (Save) |
| **`C r`** | 重新加载当前文件 (Reload) |
| **`C p`** | 模糊搜索项目文件 (Fuzzy Finder) |
| **`C j`** | 切换已打开的文件列表 |
| **`C [ / ]`** | 切换上一个/下一个工作区 |
| **`C q q`** | 退出编辑器 |
| **`tt`** | 切换回上一个激活的文件 |
| **`C g`** | 复制当前文件路径到剪贴板 |

### 编辑与导航 (Vim 风格)
| 快捷键 | 动作 |
| :--- | :--- |
| **`h / j / k / l`** | 左 / 下 / 上 / 右 移动光标 |
| **`C-f / C-b`** | 向下翻页 / 向上翻页 |
| **`L / H`** | 跳转到当前屏的 顶部 / 底部 |
| **`w / b`** | 向后 / 向前 移动一个单词 |
| **`gg / G`** | 跳转到文件 开头 / 末尾 |
| **`x / r`** | 删除字符 / 替换字符 |
| **`dd / y / p`** | 删除行 / 复制行 / 粘贴 |
| **`dw / de`** | 向后删除一个单词 (删除到空格 / 单词末尾) |
| **`cw / ce`** | 修改一个单词 (删除并进入插入模式) |
| **`J`** | 合并当前行与下一行 |
| **`> / <`** | 向右 / 向左 缩进 |
| **`zz`** | 当前行垂直居中显示 |
| **`gd`** | 跳转到定义 (Go to definition) |
| **`[[`** | 跳转到上一个函数起始位置 |
| **`.`** | 重复上一个编辑动作 |

### 模式切换
| 快捷键 | 动作 |
| :--- | :--- |
| **`i / a`** | 进入插入模式 (当前位置 / 后一位置) |
| **`I / A`** | 进入插入模式 (行首 / 行尾) |
| **`o / O`** | 在下方 / 上方插入新行并进入插入模式 |
| **`v`** | 进入视觉模式 (用于文本选择) |
| **`Esc`** | 返回常规模式 |

### 搜索与开发工具
| 快捷键 | 动作 |
| :--- | :--- |
| **`/`** | 在当前文件中搜索 |
| **`*`** | 搜索光标下的单词 |
| **`?`** | 在项目全局搜索 (Grep) |
| **`n / N`** | 下一个 / 上一个 搜索匹配项 |
| **`F5`** | 运行当前文件 |
| **`C u / y`** | 执行构建脚本 (`build` / `build2`) |
| **`C -`** | 查看 Git 差异 (Git Diff) |
| **`C c`** | 提交 Git 修改 (Git Commit) |
| **`C d / e`** | 切换到左侧 / 右侧分屏 (Split) |

### 任务与系统
| 快捷键 | 动作 |
| :--- | :--- |
| **`C 0`** | 创建新任务 (开始工作计时) |
| **`C 9`** | 创建新“非生产力”任务 (前缀自动加 `@`) |
| **`C t`** | 打开番茄钟/计时器面板 (查看当日时间分配) |
| **`C +/-`** | 调整字体大小 (需配合 Shift) |
| **`C + a`** | 将光标所在行找到的第一个数字增加 1 |

## 6. 开发计划 (Roadmap / TODO)

### 短期计划
*   **优化 UI 控件**：引入 `ui.textbox` 提升输入体验。
*   **错误反馈**：直接在发生错误的代码行上打印错误消息。
*   **状态恢复**：在切换文件（使用 `t` 或 `tt`）时自动记住并恢复之前的行位置。
*   **文件列表**：提供类似 Ctrl+P 的所有已打开文件清单。

### 长期目标
*   **完善 Vim 模式**：目前仅实现子集，未来将支持更完整的 Vim 指令。
*   **多按键方案**：计划支持 Emacs 和 Nano 的键位绑定。
*   **显示增强**：实现自动换行（Word wrap）和更强大的语法高亮解析。

---
*文档更新于：2026-01-10 (已整合 README, TODO 及源码分析结果)*
